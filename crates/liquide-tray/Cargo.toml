[package]
name = "liquide-tray"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "System tray / status notifier items for the LiquiDE desktop (StatusNotifierItem specification)"

[lib]
name = "liquide_tray"
path = "src/lib.rs"

[dependencies]
serde.workspace = true
