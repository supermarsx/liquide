[package]
name = "liquide-firewall"
version = "0.1.0"
edition = "2024"

[dependencies]
# Pre-added by t51-e1 for the Wave A3 authorization seam (t51-e4 adds the calls).
liquide-authz-runtime.workspace = true
# t51-e4: the firewall names `Subject`/`Resource` (the authorization principal
# and the rule/profile target) at its gating seam; these types live here.
liquide-authorization.workspace = true
