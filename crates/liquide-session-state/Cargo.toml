[package]
name = "liquide-session-state"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Desktop session save/restore for LiquiDE — window positions, workspaces, and display layout"

[lib]
name = "liquide_session_state"
path = "src/lib.rs"

[dependencies]
