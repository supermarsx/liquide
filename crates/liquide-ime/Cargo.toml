[package]
name = "liquide-ime"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Input Method Editor (IME) support for the LiquiDE desktop environment"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
