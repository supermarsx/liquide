[package]
name = "liquide-icons"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Vector icon database for Liquide shell"

[dependencies]
liquide-compositor = { path = "../liquide-compositor" }

[dev-dependencies]
